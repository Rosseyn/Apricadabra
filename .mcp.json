{
  "mcpServers": {
    "bitbucket": {
      "command": "npx",
      "args": ["-y", "@aashari/mcp-server-atlassian-bitbucket"],
      "env": {
        "ATLASSIAN_USER_EMAIL": "${BITBUCKET_USER_EMAIL}",
        "ATLASSIAN_API_TOKEN": "${BITBUCKET_API_TOKEN}",
        "BITBUCKET_DEFAULT_WORKSPACE": "${BITBUCKET_WORKSPACE}"
      }
    }
  }
}
